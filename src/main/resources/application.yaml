# Ghanaian Checkers - Application Configuration
spring:
  application:
    name: Ghanaian Checkers
  devtools:
    restart:
      enabled: true

server:
  port: 8080

# Vaadin configuration
vaadin:
  launch-browser: true
  whitelisted-packages: com.vaadin,com.dame

# Logging
logging:
  level:
    com.dame: DEBUG
